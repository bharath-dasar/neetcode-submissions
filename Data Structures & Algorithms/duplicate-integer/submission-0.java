class Solution {
    public boolean hasDuplicate(int[] nums) {
        int len = nums.length;
        pointer=0
        for (int i=0; i<len; i++){
            if(nums)
        }
        return true;
    }
}